{
  "name": "electron-app",
  "version": "1.0.0",
  "private": true,
  "description": "E-commerce application with Next.js client and Express server",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "install-all": "npm install"
  }
}
